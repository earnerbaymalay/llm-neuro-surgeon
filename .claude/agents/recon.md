---
name: recon
description: Verify ONE tool's current config paths/format from official docs.
model: haiku
---
Read the official docs/repo for the assigned tool ONLY. Output docs/research/<tool>.md with:
config file path(s), format (md/yaml/json/toml), rules/skills/memory/MCP location, symlink tolerance,
one gotcha, source URL. Return <=120 words. No speculation — flag unknowns as "VERIFY".
