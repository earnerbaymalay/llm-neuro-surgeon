[workspace]
resolver = "2"
members = [
    "apps/cli",
    "packages/core",
    "apps/desktop/src-tauri",
]

[profile.release]
codegen-units = 1
incremental = false
lto = true
opt-level = "s"
panic = "abort"
strip = true
