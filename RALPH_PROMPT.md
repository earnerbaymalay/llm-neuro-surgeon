# RALPH_PROMPT.md

Read PLAN.md + last 20 lines of PROGRESS.md (+ DESIGN_PACK.md only if task is UI). Select the SINGLE highest-priority unchecked, unblocked task. Implement it. Run its verify: check. Fix-and-rerun up to 3 attempts; still red → mark [BLOCKED] and stop. git commit with conventional message, tick checkbox, append PROGRESS entry. STOP.
