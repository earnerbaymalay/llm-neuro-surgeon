# OpenCode — Recon Brief

## Config file paths
- Project: `AGENTS.md` at repo root (created via `/init`)
- Settings: "OpenCode config" referenced but no paths/formats documented in fetched content

## Format
`AGENTS.md`: Markdown.

## Rules / skills / agents / memory / MCP location
- `AGENTS.md` is project-level and should be committed.
- `opencode.md`, global settings, and MCP not documented in fetched content (VERIFY).

## Symlink tolerance
Not documented (VERIFY).

## Gotcha
OpenCode docs confirm only `AGENTS.md`; treat other formats as unverified.

## Source
https://opencode.ai/docs
